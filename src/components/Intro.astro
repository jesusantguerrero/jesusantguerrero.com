---
import { Markdown } from 'astro/components';
---
<article class="intro-section">
    <section class="personal-card flex flex-col md:flex-row">
        <div class="personal-card_left w-4/12">
            <h1 class="personal-card_name font-bold text-5xl md:text-7xl -left-7 md:-left-32">
                <p class="">Jesus</p>
                <p class="pl-14 mb-0">Guerrero</p>
                <p class="text-sm pl-14 mt-5">
                    Web developer / Software Engineer / Creator
                </p>
            </h1>
        </div>

        <div class="personal-card_body text-lg space-y-10 md:space-y-5 md:px-20 px-10 mt-48 md:mt-0 md:w-8/12">
            <p>         
                I'm a system engineer, web developer from Dominican Republic who is 
                passionate about programming in general, web development.
            </p>
            <p>
                I love to learn new things, share knowledge and create (apps, (a try of) poetry ). 
                When not, I am singing Bob Marley's songs.
            </p>
        </div>
    </section>
</article>

<style lang="scss">
    article {
        background-color: var(--darken-primary-color);
        color: var(--text-color);
        display: flex;
        align-items: center;
        width: 100%;
        height: 100vh;
    }
    
    section.personal-card {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 80%;
        width: 80%;
        border-radius: 8px;
        margin: auto;
        position: relative;
        background: var(--primary-color); 

           &:after {
                content: '';
                position: absolute;
                bottom: 0;
                right: 20%;
                height: 0;
                width: 0;
                border-right: 50px solid transparent;
                border-left: 50px solid transparent;
                border-top: 50px solid transparent;
                border-bottom: 30px solid var(--darken-primary-color);
            }

            .personal-card_name {
                color: dodgerblue;
                position: absolute;
                top: 20%;
            }
        }

</style>
